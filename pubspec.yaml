name: smart_timetable
description: "Smart Timetable Generation using Flutter & Genetic Algorithm"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_analytics: ^11.0.0
  provider: ^6.0.0
  js_bindings: ^0.1.0
  cupertino_icons: ^1.0.5

  async: ^2.11.0
  boolean_selector: ^2.1.1
  characters: ^1.3.0
  clock: ^1.1.1
  collection: ^1.19.0
  fake_async: ^1.3.1
  leak_tracker: ^10.0.7
  leak_tracker_flutter_testing: ^3.0.8
  matcher: ^0.12.16+1
  material_color_utilities: ^0.11.1
  meta: ^1.15.0
  path: ^1.9.0
  source_span: ^1.10.0
  stack_trace: ^1.12.0
  stream_channel: ^2.1.2
  string_scanner: ^1.3.0
  # term_glyph removed entirely, letting flutter_test manage it
  test_api: ^0.7.3
  vm_service: ^14.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  build_runner: ^2.4.9

flutter:
  uses-material-design: true

  